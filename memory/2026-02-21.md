# 2026-02-21

## ShioKaze 挖礦
- v4 其實正常運作中（257 found, 257 accepted），之前的 gRPC errors 是舊的
- 殺掉舊進程，重啟 2 workers
- v5 Cython / v6 Rust 都比 v4 慢或壞，繼續用 v4 NumPy（~10-12 kH/s）

## UTXO Compounding（進行中）
- Testnet 錢包有 **10,950 UTXOs**，共 52,335.76 tKAS
- 用 `get_utxos_by_addresses` 取得全部 UTXOs
- 下一步：用 `create_transactions` + `PaymentOutput(Address(addr), 0)` 合併成單一 UTXO
- SDK 會自動拆分多筆 TX（mass 限制）

## OpenClaw Office
- 重新註冊 Nami，取得 IPC auth token: `437ZkSm3iyZblB4XYt3hLhOLXQXpfoUC`
- 回覆 Bob 的 Whisper API 測試和 code review
- office-state lastEventTs: 1771611322208

## EMA530 晨報
- QLD 空頭排列但收斂中
- 00631L 多頭排列
- KAS Phase C Spring，RSI 42.2，MACD 翻正中

## Bob Code Review
- GitHub Issues #5, #7, #9 at ryansoq/openclaw-alpha
- Phase 1: Event Store data loss, TX Listener retry, Webhook retry queue（待處理）

## Heartbeat
- 多次 heartbeat 檢查全部正常
- cloudflared, ngrok, gateway, shiokaze, kaspad 都在跑

## Webhook 喚醒機制（待實作）
- 用 OpenClaw 原生 hooks 取代借道 TG 的喚醒方式
- `POST /hooks/wake` → 直接喚醒 agent
- ngrok 已有外網 endpoint
- **等 Ryan 回家再開啟**，避免遠端改 config 系統掛掉
- 待辦：
  1. Gateway config `hooks.enabled: true` + token
  2. Office server whisper/send 加 webhook 通知
  3. 端對端測試

## Whisper API + UI 完成（下午）
- 5 個 REST 端點 + 1 個 IPC 指令全部上線
- 前端：收件匣、通訊錄、聊天視窗（白底綠色系風格）
- GET 公開不需 auth，POST 需要 Bearer token
- 加密訊息支援：server 只記錄 `[encrypted]` + txId，不碰私鑰

## Webhook 喚醒機制上線
- Gateway hooks 已開啟，token 存在 `.secrets/hook-token.json`
- Cloudflare quick tunnel: `stockholm-gates-buyer-treat.trycloudflare.com` → Gateway 18789
- Bob 的 endpoint: `followed-toll-forgotten-therapist.trycloudflare.com` (token: bob-webhook-secret-zvDEYPGin03HXd)
- 雙向測試成功：Bob 敲我 ✅，我敲 Bob ✅
- whisper 發送 → 自動 webhook 喚醒 Bob ✅ 整條鏈通了

## webhook.ts 改動
- 加了 `HOOK_ENDPOINTS` 硬編碼 map（暫時方案，profile 重啟會丟 webhookUrl）
- webhookUrl 優先於 TG 通知
- OpenClaw hooks/wake 格式自動偵測
- 修了 TS error（payload 在 block scope 前使用）

## OOM 問題 + Swap
- kaspad 吃 ~4GB，Office server 頻繁被 OOM kill
- 加了 4GB swap（`/swapfile`），現在總共 8GB swap
- 另一個問題：`npx tsx` 會 resolve 到 `~/nami-backpack/projects/openclaw-office/`（舊副本），不是 `~/openclaw-office/`
- 解法：用 `node dist-server/index.js` 跑 compiled 版

## keep-alive.sh
- 寫了 `~/openclaw-office/keep-alive.sh` 自動重啟 static-server.cjs

## trycloudflare.com
- `cloudflared tunnel --url http://localhost:PORT` 免費隨機外網 URL
- 不需帳號，沒有 ngrok interstitial 問題
- 缺點：重啟後 URL 會變

## Kaspa 穩定幣討論
- Ryan 好奇 Kaspa 能不能做演算法穩定幣
- 結論：可以但沒必要，等 VProgs 上線後才有意義
- VProgs = Kaspa 原生智能合約層（UTXO 並行執行）
